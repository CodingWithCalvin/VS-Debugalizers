<UserControl x:Class="CodingWithCalvin.Debugalizers.UI.Views.ImageViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Metadata Bar -->
        <Border Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="MetadataText" FontFamily="Consolas" FontSize="11"/>
                <Separator Margin="8,0"/>
                <TextBlock Text="Zoom:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <Slider x:Name="ZoomSlider" Width="100" Minimum="10" Maximum="500" Value="100"
                        VerticalAlignment="Center" ValueChanged="ZoomSlider_ValueChanged"/>
                <TextBlock x:Name="ZoomText" Text="100%" VerticalAlignment="Center" Margin="4,0,0,0" Width="40"/>
                <Button Content="Fit" Margin="8,0,0,0" Padding="8,2" Click="FitButton_Click"/>
                <Button Content="100%" Margin="4,0,0,0" Padding="8,2" Click="ActualSizeButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Image Display -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      Background="#404040">
            <Grid>
                <!-- Checkerboard pattern for transparency -->
                <Rectangle x:Name="CheckerboardBackground" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Rectangle.Fill>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,16,16" ViewportUnits="Absolute">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <GeometryDrawing Brush="#CCCCCC">
                                        <GeometryDrawing.Geometry>
                                            <RectangleGeometry Rect="0,0,16,16"/>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                    <GeometryDrawing Brush="#FFFFFF">
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <RectangleGeometry Rect="0,0,8,8"/>
                                                <RectangleGeometry Rect="8,8,8,8"/>
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <Image x:Name="ImageContent"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.LayoutTransform>
                        <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1"/>
                    </Image.LayoutTransform>
                </Image>
            </Grid>
        </ScrollViewer>

        <!-- Error Display -->
        <TextBlock x:Name="ErrorText"
                   Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Red"
                   FontSize="14"
                   Visibility="Collapsed"/>
    </Grid>
</UserControl>
