<Window x:Class="CodingWithCalvin.Debugalizers.UI.VisualizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:CodingWithCalvin.Debugalizers.UI.Views"
        Title="Debugalizers"
        Width="900"
        Height="600"
        MinWidth="600"
        MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">

    <Window.Resources>
        <Style x:Key="ToolBarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="ViewTabStyle" TargetType="RadioButton">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <Button x:Name="CopyButton" Content="Copy" ToolTip="Copy to clipboard" Style="{StaticResource ToolBarButtonStyle}" Click="CopyButton_Click"/>
            <Button x:Name="CopyFormattedButton" Content="Copy Formatted" ToolTip="Copy formatted content" Style="{StaticResource ToolBarButtonStyle}" Click="CopyFormattedButton_Click"/>
            <Separator/>
            <Button x:Name="ExportButton" Content="Export..." ToolTip="Export to file" Style="{StaticResource ToolBarButtonStyle}" Click="ExportButton_Click"/>
            <Separator/>
            <CheckBox x:Name="WordWrapCheckBox" Content="Word Wrap" VerticalAlignment="Center" Margin="4,0" Checked="WordWrapCheckBox_Changed" Unchecked="WordWrapCheckBox_Changed"/>
            <Separator/>
            <TextBox x:Name="SearchBox" Width="200" VerticalAlignment="Center" Margin="4,0"
                     Text="" ToolTip="Search (Ctrl+F)"
                     KeyDown="SearchBox_KeyDown"/>
            <Button x:Name="SearchButton" Content="Find" Style="{StaticResource ToolBarButtonStyle}" Click="SearchButton_Click"/>
        </ToolBar>

        <!-- View Tabs -->
        <StackPanel x:Name="ViewTabsPanel" Grid.Row="1" Orientation="Horizontal" Margin="4,4,4,0"/>

        <!-- Content Area -->
        <ContentControl x:Name="ContentArea" Grid.Row="2" Margin="4"/>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="StatisticsText" Text=""/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="ValidationText" Text="" Foreground="Green"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
